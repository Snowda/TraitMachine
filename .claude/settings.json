{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "read -r input; cmd=$(echo \"$input\" | sed 's/.*\"command\"\\s*:\\s*\"//;s/\".*//'); if echo \"$cmd\" | grep -qE '^(python3?|pip3?|py) '; then echo '{\"decision\":\"block\",\"reason\":\"Blocked: Python is disabled in this Rust project. Use Rust or shell commands.\"}'; fi",
            "shell": "bash",
            "timeout": 5,
            "statusMessage": "Checking for Python commands..."
          }
        ]
      }
    ]
  }
}
